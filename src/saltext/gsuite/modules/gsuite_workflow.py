# DO NOT EDIT - generated by tools/generate_modules.py. Changes will be overwritten.
# Parameter names mirror the Google API reference verbatim (often camelCase); long Discovery
# descriptions and URLs live in the docstrings.
# pylint: disable=invalid-name,line-too-long,redefined-builtin,redefined-outer-name,too-many-lines
"""
Cross-service productivity workflows execution module.

Cross-service productivity workflows

This module is generated from the Google Discovery document for ``workflow/v1``
and drives the bundled ``gws`` CLI. Parameter names match the Google API reference verbatim;
optional parameters are passed through ``**params``.
"""

import logging

from saltext.gsuite import _binary
from saltext.gsuite import _gws

log = logging.getLogger(__name__)

__virtualname__ = "gsuite_workflow"


def __virtual__():
    if not _binary.is_available(opts=__opts__, pillar=__pillar__):
        return (
            False,
            "The 'gws' binary is not available; install the platform-specific "
            "saltext.gsuite wheel or set gsuite:binary_path.",
        )
    return __virtualname__


def email_to_task(message_id, tasklist=None, test=False):
    """
    [Helper] Convert a Gmail message into a Google Tasks entry

    Options:

    * ``message_id`` (required value): Gmail message ID to convert
    * ``tasklist`` (optional value): Task list ID (default: @default) [default: @default]

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_workflow.email_to_task
    """
    return _gws.run_helper(
        __opts__,
        __pillar__,
        "workflow",
        "+email-to-task",
        flags={
            "message-id": message_id,
            "tasklist": tasklist,
        },
        test=test,
    )


def file_announce(file_id, space, message=None, test=False):
    """
    [Helper] Announce a Drive file in a Chat space

    Options:

    * ``file_id`` (required value): Drive file ID to announce
    * ``space`` (required value): Chat space name (e.g. spaces/SPACE\\_ID)
    * ``message`` (optional value): Custom announcement message

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_workflow.file_announce
    """
    return _gws.run_helper(
        __opts__,
        __pillar__,
        "workflow",
        "+file-announce",
        flags={
            "file-id": file_id,
            "space": space,
            "message": message,
        },
        test=test,
    )


def meeting_prep(calendar=None, test=False):
    """
    [Helper] Prepare for your next meeting: agenda, attendees, and linked docs

    Options:

    * ``calendar`` (optional value): Calendar ID (default: primary) [default: primary]

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_workflow.meeting_prep
    """
    return _gws.run_helper(
        __opts__,
        __pillar__,
        "workflow",
        "+meeting-prep",
        flags={
            "calendar": calendar,
        },
        test=test,
    )


def standup_report(test=False):
    """
    [Helper] Today's meetings + open tasks as a standup summary

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_workflow.standup_report
    """
    return _gws.run_helper(
        __opts__,
        __pillar__,
        "workflow",
        "+standup-report",
        test=test,
    )


def weekly_digest(test=False):
    """
    [Helper] Weekly summary: this week's meetings + unread email count

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_workflow.weekly_digest
    """
    return _gws.run_helper(
        __opts__,
        __pillar__,
        "workflow",
        "+weekly-digest",
        test=test,
    )
