# DO NOT EDIT - generated by tools/generate_modules.py. Changes will be overwritten.
# Parameter names mirror the Google API reference verbatim (often camelCase); long Discovery
# descriptions and URLs live in the docstrings.
# pylint: disable=invalid-name,line-too-long,redefined-builtin,redefined-outer-name,too-many-lines
"""
Manage Google Meet conferences execution module.

Manage Google Meet conferences

This module is generated from the Google Discovery document for ``meet/v2``
and drives the bundled ``gws`` CLI. Parameter names match the Google API reference verbatim;
optional parameters are passed through ``**params``.
"""

import logging

from saltext.gsuite import _binary
from saltext.gsuite import _gws

log = logging.getLogger(__name__)

__virtualname__ = "gsuite_meet"


def __virtual__():
    if not _binary.is_available(opts=__opts__, pillar=__pillar__):
        return (
            False,
            "The 'gws' binary is not available; install the platform-specific "
            "saltext.gsuite wheel or set gsuite:binary_path.",
        )
    return __virtualname__


def conference_records_get(name, **params):
    """
    Gets a conference record by conference ID.

    HTTP: ``GET``  Discovery: ``conferenceRecords.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the conference.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords"],
        "get",
        params=_params or None,
    )


def conference_records_list(**params):
    """
    Lists the conference records. By default, ordered by start time and in descending
    order.

    HTTP: ``GET``  Discovery: ``conferenceRecords.list``

    Optional parameters (pass via keyword args):

    * ``filter`` (str): Optional. User specified filtering condition in [EBNF format](https://en.wikipedia.org/wiki/Extended\_Backus%E2%80%93Naur\_form). The following are the filterable fields: \* \`space.meeting\_code\` \* \`spac…
    * ``pageSize`` (int): Optional. Maximum number of conference records to return. The service might return fewer than this value. If unspecified, at most 25 conference records are returned. The maximum value is 100; values…
    * ``pageToken`` (str): Optional. Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_list
    """
    _params = dict(params)
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords"],
        "list",
        params=_params or None,
    )


def conference_records_participants_get(name, **params):
    """
    Gets a participant by participant ID.

    HTTP: ``GET``  Discovery: ``conferenceRecords.participants.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the participant.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_participants_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "participants"],
        "get",
        params=_params or None,
    )


def conference_records_participants_list(parent, **params):
    """
    Lists the participants in a conference record. By default, ordered by join time and
    in descending order. This API supports \`fields\` as standard parameters like every
    other API. However, when the \`fields\` request parameter is omitted, this API
    defaults to \`'participants/\*, next\_page\_token'\`.

    HTTP: ``GET``  Discovery: ``conferenceRecords.participants.list``

    Required parameters:

    * ``parent`` (str): Required. Format: \`conferenceRecords/{conference\_record}\`

    Optional parameters (pass via keyword args):

    * ``filter`` (str): Optional. User specified filtering condition in [EBNF format](https://en.wikipedia.org/wiki/Extended\_Backus%E2%80%93Naur\_form). The following are the filterable fields: \* \`earliest\_start\_time\` \* \`lat…
    * ``pageSize`` (int): Maximum number of participants to return. The service might return fewer than this value. If unspecified, at most 100 participants are returned. The maximum value is 250; values above 250 are coerced…
    * ``pageToken`` (str): Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_participants_list parent=<str>
    """
    _params = dict(params)
    _params["parent"] = parent
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "participants"],
        "list",
        params=_params or None,
    )


def conference_records_participants_participant_sessions_get(name, **params):
    """
    Gets a participant session by participant session ID.

    HTTP: ``GET``  Discovery: ``conferenceRecords.participants.participantSessions.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the participant.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_participants_participant_sessions_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "participants", "participantSessions"],
        "get",
        params=_params or None,
    )


def conference_records_participants_participant_sessions_list(parent, **params):
    """
    Lists the participant sessions of a participant in a conference record. By default,
    ordered by join time and in descending order. This API supports \`fields\` as
    standard parameters like every other API. However, when the \`fields\` request
    parameter is omitted this API defaults to \`'participantsessions/\*,
    next\_page\_token'\`.

    HTTP: ``GET``  Discovery: ``conferenceRecords.participants.participantSessions.list``

    Required parameters:

    * ``parent`` (str): Required. Format: \`conferenceRecords/{conference\_record}/participants/{participant}\`

    Optional parameters (pass via keyword args):

    * ``filter`` (str): Optional. User specified filtering condition in [EBNF format](https://en.wikipedia.org/wiki/Extended\_Backus%E2%80%93Naur\_form). The following are the filterable fields: \* \`start\_time\` \* \`end\_time\` Fo…
    * ``pageSize`` (int): Optional. Maximum number of participant sessions to return. The service might return fewer than this value. If unspecified, at most 100 participants are returned. The maximum value is 250; values abo…
    * ``pageToken`` (str): Optional. Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_participants_participant_sessions_list parent=<str>
    """
    _params = dict(params)
    _params["parent"] = parent
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "participants", "participantSessions"],
        "list",
        params=_params or None,
    )


def conference_records_recordings_get(name, **params):
    """
    Gets a recording by recording ID.

    HTTP: ``GET``  Discovery: ``conferenceRecords.recordings.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the recording.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_recordings_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "recordings"],
        "get",
        params=_params or None,
    )


def conference_records_recordings_list(parent, **params):
    """
    Lists the recording resources from the conference record. By default, ordered by
    start time and in ascending order.

    HTTP: ``GET``  Discovery: ``conferenceRecords.recordings.list``

    Required parameters:

    * ``parent`` (str): Required. Format: \`conferenceRecords/{conference\_record}\`

    Optional parameters (pass via keyword args):

    * ``pageSize`` (int): Maximum number of recordings to return. The service might return fewer than this value. If unspecified, at most 10 recordings are returned. The maximum value is 100; values above 100 are coerced to 1…
    * ``pageToken`` (str): Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_recordings_list parent=<str>
    """
    _params = dict(params)
    _params["parent"] = parent
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "recordings"],
        "list",
        params=_params or None,
    )


def conference_records_smart_notes_get(name, **params):
    """
    Gets smart notes by smart note ID.

    HTTP: ``GET``  Discovery: ``conferenceRecords.smartNotes.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the smart note. Format: conferenceRecords/{conference\_record}/smartNotes/{smart\_note}

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_smart_notes_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "smartNotes"],
        "get",
        params=_params or None,
    )


def conference_records_smart_notes_list(parent, **params):
    """
    Lists the set of smart notes from the conference record. By default, ordered by
    start time and in ascending order.

    HTTP: ``GET``  Discovery: ``conferenceRecords.smartNotes.list``

    Required parameters:

    * ``parent`` (str): Required. Format: \`conferenceRecords/{conference\_record}\`

    Optional parameters (pass via keyword args):

    * ``pageSize`` (int): Optional. Maximum number of smart notes to return. The service might return fewer than this value. If unspecified, at most 10 smart notes are returned. The maximum value is 100; values above 100 are…
    * ``pageToken`` (str): Optional. Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_smart_notes_list parent=<str>
    """
    _params = dict(params)
    _params["parent"] = parent
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "smartNotes"],
        "list",
        params=_params or None,
    )


def conference_records_transcripts_get(name, **params):
    """
    Gets a transcript by transcript ID.

    HTTP: ``GET``  Discovery: ``conferenceRecords.transcripts.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the transcript.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_transcripts_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "transcripts"],
        "get",
        params=_params or None,
    )


def conference_records_transcripts_list(parent, **params):
    """
    Lists the set of transcripts from the conference record. By default, ordered by
    start time and in ascending order.

    HTTP: ``GET``  Discovery: ``conferenceRecords.transcripts.list``

    Required parameters:

    * ``parent`` (str): Required. Format: \`conferenceRecords/{conference\_record}\`

    Optional parameters (pass via keyword args):

    * ``pageSize`` (int): Maximum number of transcripts to return. The service might return fewer than this value. If unspecified, at most 10 transcripts are returned. The maximum value is 100; values above 100 are coerced to…
    * ``pageToken`` (str): Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_transcripts_list parent=<str>
    """
    _params = dict(params)
    _params["parent"] = parent
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "transcripts"],
        "list",
        params=_params or None,
    )


def conference_records_transcripts_entries_get(name, **params):
    """
    Gets a \`TranscriptEntry\` resource by entry ID. Note: The transcript entries
    returned by the Google Meet API might not match the transcription found in the
    Google Docs transcript file. This can occur when 1) we have interleaved speakers
    within milliseconds, or 2) the Google Docs transcript file is modified after
    generation.

    HTTP: ``GET``  Discovery: ``conferenceRecords.transcripts.entries.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the \`TranscriptEntry\`.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_transcripts_entries_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "transcripts", "entries"],
        "get",
        params=_params or None,
    )


def conference_records_transcripts_entries_list(parent, **params):
    """
    Lists the structured transcript entries per transcript. By default, ordered by start
    time and in ascending order. Note: The transcript entries returned by the Google
    Meet API might not match the transcription found in the Google Docs transcript file.
    This can occur when 1) we have interleaved speakers within milliseconds, or 2) the
    Google Docs transcript file is modified after generation.

    HTTP: ``GET``  Discovery: ``conferenceRecords.transcripts.entries.list``

    Required parameters:

    * ``parent`` (str): Required. Format: \`conferenceRecords/{conference\_record}/transcripts/{transcript}\`

    Optional parameters (pass via keyword args):

    * ``pageSize`` (int): Maximum number of entries to return. The service might return fewer than this value. If unspecified, at most 10 entries are returned. The maximum value is 100; values above 100 are coerced to 100. Ma…
    * ``pageToken`` (str): Page token returned from previous List Call.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.conference_records_transcripts_entries_list parent=<str>
    """
    _params = dict(params)
    _params["parent"] = parent
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["conferenceRecords", "transcripts", "entries"],
        "list",
        params=_params or None,
    )


def spaces_create(body=None, **params):
    """
    Creates a space.

    HTTP: ``POST``  Discovery: ``spaces.create``

    * ``body`` (dict): request body payload.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.spaces_create
    """
    _params = dict(params)
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["spaces"],
        "create",
        params=_params or None,
        body=body,
    )


def spaces_end_active_conference(name, body=None, **params):
    """
    Ends an active conference (if there's one). For an example, see [End active
    conference](https://developers.google.com/workspace/meet/api/guides/meeting-
    spaces#end-active-conference).

    HTTP: ``POST``  Discovery: ``spaces.endActiveConference``

    Required parameters:

    * ``name`` (str): Required. Resource name of the space. Format: \`spaces/{space}\`. \`{space}\` is the resource identifier for the space. It's a unique, server-generated ID and is case sensitive. For example, \`jQCFfuBOdN5…

    * ``body`` (dict): request body payload.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.spaces_end_active_conference name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["spaces"],
        "endActiveConference",
        params=_params or None,
        body=body,
    )


def spaces_get(name, **params):
    """
    Gets details about a meeting space. For an example, see [Get a meeting
    space](https://developers.google.com/workspace/meet/api/guides/meeting-spaces#get-
    meeting-space).

    HTTP: ``GET``  Discovery: ``spaces.get``

    Required parameters:

    * ``name`` (str): Required. Resource name of the space. Format: \`spaces/{space}\` or \`spaces/{meetingCode}\`. \`{space}\` is the resource identifier for the space. It's a unique, server-generated ID and is case sensitive.…

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.spaces_get name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["spaces"],
        "get",
        params=_params or None,
    )


def spaces_patch(name, body=None, **params):
    """
    Updates details about a meeting space. For an example, see [Update a meeting
    space](https://developers.google.com/workspace/meet/api/guides/meeting-
    spaces#update-meeting-space).

    HTTP: ``PATCH``  Discovery: ``spaces.patch``

    Required parameters:

    * ``name`` (str): Immutable. Resource name of the space. Format: \`spaces/{space}\`. \`{space}\` is the resource identifier for the space. It's a unique, server-generated ID and is case sensitive. For example, \`jQCFfuBOdN…

    * ``body`` (dict): request body payload.

    Optional parameters (pass via keyword args):

    * ``updateMask`` (str): Optional. Field mask used to specify the fields to be updated in the space. If update\_mask isn't provided(not set, set with empty paths, or only has "" as paths), it defaults to update all fields pro…

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_meet.spaces_patch name=<str>
    """
    _params = dict(params)
    _params["name"] = name
    return _gws.call(
        __opts__,
        __pillar__,
        "meet",
        ["spaces"],
        "patch",
        params=_params or None,
        body=body,
    )
