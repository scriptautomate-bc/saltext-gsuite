# DO NOT EDIT - generated by tools/generate_modules.py. Changes will be overwritten.
# Parameter names mirror the Google API reference verbatim (often camelCase); long Discovery
# descriptions and URLs live in the docstrings.
# pylint: disable=invalid-name,line-too-long,redefined-builtin,redefined-outer-name,too-many-lines
"""
Read and write Google Docs execution module.

Read and write Google Docs

This module is generated from the Google Discovery document for ``docs/v1``
and drives the bundled ``gws`` CLI. Parameter names match the Google API reference verbatim;
optional parameters are passed through ``**params``.
"""

import logging

from saltext.gsuite import _binary
from saltext.gsuite import _gws

log = logging.getLogger(__name__)

__virtualname__ = "gsuite_docs"


def __virtual__():
    if not _binary.is_available(opts=__opts__, pillar=__pillar__):
        return (
            False,
            "The 'gws' binary is not available; install the platform-specific "
            "saltext.gsuite wheel or set gsuite:binary_path.",
        )
    return __virtualname__


def documents_batch_update(documentId, body=None, **params):
    """
    Applies one or more updates to the document. Each request is validated before being
    applied. If any request is not valid, then the entire request will fail and nothing
    will be applied. Some requests have replies to give you some information about how
    they are applied. Other requests do not need to return information; these each
    return an empty reply. The order of replies matches that of the requests. For
    example, suppose you call batchUpdate with four updates, and only the third one
    returns information. The response would have two empty replies, the reply to the
    third request, and another empty reply, in that order. Because other users may be
    editing the document, the document might not exactly reflect your changes: your
    changes may be altered with respect to collaborator changes. If there are no
    collaborators, the document should reflect your changes. In any case, the updates in
    your request are guaranteed to be applied together atomically.

    HTTP: ``POST``  Discovery: ``documents.batchUpdate``

    Required parameters:

    * ``documentId`` (str): The ID of the document to update.

    * ``body`` (dict): request body payload.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_docs.documents_batch_update documentId=<str>
    """
    _params = dict(params)
    _params["documentId"] = documentId
    return _gws.call(
        __opts__,
        __pillar__,
        "docs",
        ["documents"],
        "batchUpdate",
        params=_params or None,
        body=body,
    )


def documents_create(body=None, **params):
    """
    Creates a blank document using the title given in the request. Other fields in the
    request, including any provided content, are ignored. Returns the created document.

    HTTP: ``POST``  Discovery: ``documents.create``

    * ``body`` (dict): request body payload.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_docs.documents_create
    """
    _params = dict(params)
    return _gws.call(
        __opts__,
        __pillar__,
        "docs",
        ["documents"],
        "create",
        params=_params or None,
        body=body,
    )


def documents_get(documentId, **params):
    """
    Gets the latest version of the specified document.

    HTTP: ``GET``  Discovery: ``documents.get``

    Required parameters:

    * ``documentId`` (str): The ID of the document to retrieve.

    Optional parameters (pass via keyword args):

    * ``includeTabsContent`` (bool): Whether to populate the Document.tabs field instead of the text content fields like \`body\` and \`documentStyle\` on Document. - When \`True\`: Document content populates in the Document.tabs field instea…
    * ``suggestionsViewMode`` (str): The suggestions view mode to apply to the document. This allows viewing the document with all suggestions inline, accepted or rejected. If one is not specified, DEFAULT\_FOR\_CURRENT\_ACCESS is used.

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_docs.documents_get documentId=<str>
    """
    _params = dict(params)
    _params["documentId"] = documentId
    return _gws.call(
        __opts__,
        __pillar__,
        "docs",
        ["documents"],
        "get",
        params=_params or None,
    )


def write(document, text, test=False):
    """
    [Helper] Append text to a document

    Options:

    * ``document`` (required value): Document ID
    * ``text`` (required value): Text to append (plain text)

    CLI Example:

    .. code-block:: bash

        salt '*' gsuite_docs.write
    """
    return _gws.run_helper(
        __opts__,
        __pillar__,
        "docs",
        "+write",
        flags={
            "document": document,
            "text": text,
        },
        test=test,
    )
