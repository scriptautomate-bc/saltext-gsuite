# DO NOT EDIT - generated by tools/generate_modules.py. Changes will be overwritten.
# pylint: disable=invalid-name,line-too-long,broad-except
"""
Subscribe to Google Workspace events state module (best-effort, generated).

Auto-generated ``present``/``absent`` states derived from the CRUD method pattern of each
resource. Identity/diffing is shallow; resources that need real idempotency logic should be
hand-tuned in ``saltext/gsuite/states/overrides/events.py`` (which the generator prefers
over the auto-generated version).
"""

import logging

from saltext.gsuite import _binary

log = logging.getLogger(__name__)

__virtualname__ = "gsuite_events"


def __virtual__():
    if not _binary.is_available(opts=__opts__, pillar=__pillar__):
        return (False, "The 'gws' binary is not available")
    return __virtualname__


def subscriptions_present(name, body=None, **params):
    """
    Ensure the subscriptions identified by ``name`` exists (best-effort).

    ``name`` is the value of the ``name`` identifier. Additional required
    parameters (e.g. parent identifiers) and query parameters are passed via ``**params``.
    ``body`` is the resource representation used on create/update.

    CLI Example:

    .. code-block:: yaml

        my-subscriptions_present:
          gsuite_events.subscriptions_present:
            - name: name-value
            - body: {}
    """
    ret = {"name": name, "changes": {}, "result": None, "comment": ""}
    test = __opts__.get("test", False)
    lookup = dict(params)
    lookup["name"] = name
    existing = None
    try:
        existing = __salt__["gsuite_events.subscriptions_get"](**lookup)
    except Exception:  # pylint: disable=broad-except
        existing = None

    if existing:
        if body:
            if test:
                ret["result"] = None
                ret["changes"] = {"updated": name}
                ret["comment"] = f"subscriptions '{name}' would be updated"
                return ret
            update_args = dict(params)
            update_args["name"] = name
            __salt__["gsuite_events.subscriptions_patch"](body=body, **update_args)
            ret["result"] = True
            ret["changes"] = {"updated": name}
            ret["comment"] = f"subscriptions '{name}' updated"
            return ret
        ret["result"] = True
        ret["comment"] = f"subscriptions '{name}' is already present"
        return ret

    if test:
        ret["result"] = None
        ret["changes"] = {"created": name}
        ret["comment"] = f"subscriptions '{name}' would be created"
        return ret
    create_args = dict(params)
    __salt__["gsuite_events.subscriptions_create"](body=body, **create_args)
    ret["result"] = True
    ret["changes"] = {"created": name}
    ret["comment"] = f"subscriptions '{name}' created"
    return ret


def subscriptions_absent(name, **params):
    """
    Ensure the subscriptions identified by ``name`` is absent (best-effort).

    CLI Example:

    .. code-block:: yaml

        remove-subscriptions_absent:
          gsuite_events.subscriptions_absent:
            - name: name-value
    """
    ret = {"name": name, "changes": {}, "result": None, "comment": ""}
    test = __opts__.get("test", False)
    lookup = dict(params)
    lookup["name"] = name
    existing = None
    try:
        existing = __salt__["gsuite_events.subscriptions_get"](**lookup)
    except Exception:  # pylint: disable=broad-except
        existing = None

    if not existing:
        ret["result"] = True
        ret["comment"] = f"subscriptions '{name}' is already absent"
        return ret
    if test:
        ret["result"] = None
        ret["changes"] = {"deleted": name}
        ret["comment"] = f"subscriptions '{name}' would be deleted"
        return ret
    __salt__["gsuite_events.subscriptions_delete"](**lookup)
    ret["result"] = True
    ret["changes"] = {"deleted": name}
    ret["comment"] = f"subscriptions '{name}' deleted"
    return ret


def tasks_push_notification_configs_present(name, body=None, **params):
    """
    Ensure the pushNotificationConfigs identified by ``name`` exists (best-effort).

    ``name`` is the value of the ``name`` identifier. Additional required
    parameters (e.g. parent identifiers) and query parameters are passed via ``**params``.
    ``body`` is the resource representation used on create.

    CLI Example:

    .. code-block:: yaml

        my-tasks_push_notification_configs_present:
          gsuite_events.tasks_push_notification_configs_present:
            - name: name-value
            - body: {}
    """
    ret = {"name": name, "changes": {}, "result": None, "comment": ""}
    test = __opts__.get("test", False)
    lookup = dict(params)
    lookup["name"] = name
    existing = None
    try:
        existing = __salt__["gsuite_events.tasks_push_notification_configs_get"](**lookup)
    except Exception:  # pylint: disable=broad-except
        existing = None

    if existing:
        ret["result"] = True
        ret["comment"] = f"pushNotificationConfigs '{name}' is already present"
        return ret

    if test:
        ret["result"] = None
        ret["changes"] = {"created": name}
        ret["comment"] = f"pushNotificationConfigs '{name}' would be created"
        return ret
    create_args = dict(params)
    __salt__["gsuite_events.tasks_push_notification_configs_create"](body=body, **create_args)
    ret["result"] = True
    ret["changes"] = {"created": name}
    ret["comment"] = f"pushNotificationConfigs '{name}' created"
    return ret


def tasks_push_notification_configs_absent(name, **params):
    """
    Ensure the pushNotificationConfigs identified by ``name`` is absent (best-effort).

    CLI Example:

    .. code-block:: yaml

        remove-tasks_push_notification_configs_absent:
          gsuite_events.tasks_push_notification_configs_absent:
            - name: name-value
    """
    ret = {"name": name, "changes": {}, "result": None, "comment": ""}
    test = __opts__.get("test", False)
    lookup = dict(params)
    lookup["name"] = name
    existing = None
    try:
        existing = __salt__["gsuite_events.tasks_push_notification_configs_get"](**lookup)
    except Exception:  # pylint: disable=broad-except
        existing = None

    if not existing:
        ret["result"] = True
        ret["comment"] = f"pushNotificationConfigs '{name}' is already absent"
        return ret
    if test:
        ret["result"] = None
        ret["changes"] = {"deleted": name}
        ret["comment"] = f"pushNotificationConfigs '{name}' would be deleted"
        return ret
    __salt__["gsuite_events.tasks_push_notification_configs_delete"](**lookup)
    ret["result"] = True
    ret["changes"] = {"deleted": name}
    ret["comment"] = f"pushNotificationConfigs '{name}' deleted"
    return ret
